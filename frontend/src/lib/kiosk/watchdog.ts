/**
 * Placeholder for kiosk self-healing (reconnect / wake). initKioskSync calls this;
 * real behavior can be added when the store is wired to the live socket.
 */
export function initWatchdog(): void {
  /* noop */
}
